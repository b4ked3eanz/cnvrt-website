{
  "summary": "Exact physically-based glass: thickness, absorption, Fresnel, dispersion, TIR — for a screen-space DOM refraction pipeline",
  "agentCount": 7,
  "logs": [],
  "result": "All three shaders compile clean as ESSL 1.00 behind the real r185 fragment prefix, and the negative control fails exactly where predicted (`ERROR: 'projectionMatrix' : undeclared identifier`). Here is the spec.\n\n---\n\n# Physically real glass in the existing 3-pass pipeline\n### Implementation spec — `index.html`, offerings glass ring\n\nEverything below is a delta against the code that is in the file today. Line numbers are current. Every shader in this document was compiled with `glslangValidator 11.7.0` as GLSL ES 1.00 behind the exact three r185 `prefixFragment`, and the negative controls were run.\n\n---\n\n## 0. Six defects in the current code, in order of how much they cost\n\n| # | Where | Defect |\n|---|---|---|\n| 0.1 | `index.html:8482` + `LIT_F` | `outputColorSpace = LinearSRGBColorSpace`, so the lit canvas writes **linear** values into a buffer CSS composites as **sRGB**. Measured: an intended 0.04 shows as linear 0.0031 — **12.9× too dark**. At 0.10 it is 10.0× too dark. This is why `bright: 1.41` exists on the backdrop, and why every \"reflectance\" slider is cranked past 1. |\n| 0.2 | `index.html:8785` | The back-depth RT is cleared with `setClearColor(0x000000, **1**)`. Alpha is 1 everywhere, so there is no hit flag. Where the MSAA'd front pass covers a pixel the single-sampled back RT missed, `backD` reads the clear value `0` and thickness evaluates to `camD − zFront ≈ +34` units. That is a one-pixel ring of near-maximum displacement running around the entire silhouette. |\n| 0.3 | `index.html:8869` | `uni.uThick.value = P.thick * P.scale`. `thick` stopped being a length when the back-face pass landed — it is a dimensionless multiplier now, and the measured `dz` already scales with the mesh. Growing the ring scales thickness **twice**. |\n| 0.4 | `LIT_F` | Absorption is computed in a layer composited `mix-blend-mode: screen`. Screen can only add. **Beer-Lambert cannot darken the DOM from there** — it is currently only attenuating the glass's own reflections. |\n| 0.5 | `applyEncoded`, `index.html:9048-9050` | Dispersion is `S*(1−chroma) / S / S*(1+chroma)` — symmetric. Real dispersion is asymmetric: blue deviates **2.017×** as far from green as red does. Symmetric weights slide green off-centre by half the spread, which tints the whole refracted image instead of only fringing its edges. |\n| 0.6 | `ENC_F`, `LIT_F`, `matMask` | Fresnel is `pow(1−NdotV, uFresP)` with `ior: 1.185` → F0 = **0.72%**. There is no IOR/F0 relationship at all, the exponent is a free parameter, and 1.185 is not a material — it is a strength knob wearing an optics label. |\n\n---\n\n## A. The back-face depth pass\n\nThe pass already exists (`matBack`, `renderBack`, `index.html:8731–8797`). Four changes, one of which is free and unlocks TIR.\n\n### A.1 Render target\n\n```js\n// capability guard — HalfFloatType is not renderable without one of these,\n// and an incomplete framebuffer fails silently here (the try/catch around the\n// three import means you just get the flat film back with no error).\nvar CAN_MEASURE = renderer.extensions.has('EXT_color_buffer_half_float')\n               || renderer.extensions.has('EXT_color_buffer_float');\n\nbackRT = new THREE.WebGLRenderTarget(w, h, {\n  format:          THREE.RGBAFormat,       // -> RGBA16F\n  type:            THREE.HalfFloatType,\n  minFilter:       THREE.NearestFilter,    // NEAREST => OES_texture_float_linear not needed\n  magFilter:       THREE.NearestFilter,\n  generateMipmaps: false,\n  depthBuffer:     true,                   // the depth TEST, not a depth texture\n  stencilBuffer:   false,\n  colorSpace:      THREE.NoColorSpace      // these are numbers, not colour\n});\n// samples: LEAVE AT THE DEFAULT 0. A multisampled colour attachment resolves by\n// AVERAGING, which along the silhouette blends a real back-face depth with the\n// clear value and fabricates a one-pixel thickness ring exactly on the bevel.\n```\n\nHalf-float is sufficient **only because of the bias**. IEEE binary16 ulp is 1.0 at 1200 and 0.03125 at 50. The camera sits at `(BOX/2)/tan(fov/2)` ≈ **1646** units and the solid is `(depth + 2·bevelT)·scale` = **68.2** units deep. A raw view-Z quantises to whole design pixels and the thickness bands visibly. Subtracting `uCamD` first puts it in a ±100 band precise to ~0.06. The existing comment at `index.html:8724` is correct; keep it.\n\n### A.2 Clear with alpha 0 — this is the hit flag\n\n```js\nrenderer.setClearColor(0x000000, 0);   // WAS (0x000000, 1). Alpha IS the coverage flag.\n```\nThe fragment shader keeps writing `a = 1`, so alpha reads 1 where a back face exists and 0 where none does. Without this every `step(0.5, bk.a)` test below is a no-op.\n\n### A.3 Carry the back-face NORMAL in the two spare channels\n\n`.g` and `.b` of an RGBA16F target are sitting empty. Filling them costs one `varying vec3` and buys the entire second interface — which is where total internal reflection lives, and TIR is the visual signature of thick glass.\n\n```glsl\n/* ---- BACK_V ---- */\nuniform float uCamD;\nvarying float vD;\nvarying vec3  vNb;\nvoid main(){\n  vec4 mv = modelViewMatrix * vec4(position, 1.0);\n  /* three view space is right-handed with -Z into the screen, so viewZ is\n     negative in front of the camera. -mv.z is distance from the camera plane.\n     Store it BIASED — see A.1. */\n  vD  = -mv.z - uCamD;\n  vNb = normalize(normalMatrix * normal);\n  gl_Position = projectionMatrix * mv;\n}\n\n/* ---- BACK_F ---- */\nprecision highp float;\nvarying float vD;\nvarying vec3  vNb;\nvoid main(){\n  vec3 nb = normalize(vNb);\n  gl_FragColor = vec4(vD, nb.x, nb.y, 1.0);   // A = 1 -> \"a back face is here\"\n}\n```\n\nZ is reconstructed in the front pass as `-sqrt(1 - x² - y²)`. The sign is not a guess: a back face's outward normal points away from the camera, so `n.z < 0` in view space by construction. At the silhouette `|n.z| → 0` and the sign is irrelevant.\n\nDo **not** flip the normal in the back pass. `side: THREE.BackSide` does not call `gl.cullFace(gl.FRONT)` — `WebGLState.setMaterial` sets `flipSided = (side === BackSide)` and issues `gl.frontFace(gl.CW)`. The winding is reversed, so `gl_FrontFacing` is **inverted** under BackSide. Never write a `gl_FrontFacing`-based sign test in this material.\n\nKeep `side: THREE.BackSide` and the default `LessEqualDepth`. That yields the **nearest** back face, which is the true first exit for any closed manifold: walking the ray from the eye, the first crossing is a front face and the next is a back face.\n\n### A.4 Getting true view-space thickness in the front pass\n\nShared prelude, included by all three front shaders:\n\n```glsl\nprecision highp float;\n#define PI 3.141592653589793\n\nuniform float uIor, uCamD, uAttenDist;\nuniform vec3  uAtten;\nuniform sampler2D uBack;\nuniform vec2  uRes;    // DRAWING-BUFFER pixels (setPixelRatio(1), so == setSize)\nuniform vec2  uTexel;  // 1.0 / uRes\n\nvarying vec3 vN; varying vec3 vP; varying float vG; varying vec2 vRad;\n\nvec2 backAt(vec2 su){                       // -> (axial back distance, hit)\n  vec4 b = texture2D(uBack, su);\n  return vec2(b.r + uCamD, b.a);\n}\n\n/* THE SILHOUETTE, PART 1 — coverage rescue.\n   The front passes draw into the MSAA default framebuffer; the back RT is\n   single-sampled. A fragment on the outline can be covered in one and not the\n   other. Without a rescue that fragment gets dz = 0; without a HIT FLAG it gets\n   dz = camD - zFront, which is ~34 units of invented solid. One-texel cross,\n   valid-weighted. */\nvec2 backRescue(vec2 su){\n  vec2 b = backAt(su);\n  if (b.y > 0.5) return b;\n  vec2 a1 = backAt(su + vec2( uTexel.x, 0.0));\n  vec2 a2 = backAt(su + vec2(-uTexel.x, 0.0));\n  vec2 a3 = backAt(su + vec2(0.0,  uTexel.y));\n  vec2 a4 = backAt(su + vec2(0.0, -uTexel.y));\n  float w = a1.y + a2.y + a3.y + a4.y;\n  if (w < 0.5) return vec2(0.0, 0.0);\n  return vec2((a1.x*a1.y + a2.x*a2.y + a3.x*a3.y + a4.x*a4.y) / w, 1.0);\n}\n\nfloat F_Schlick(float u, float f0){\n  float m = clamp(1.0 - u, 0.0, 1.0); float m2 = m*m;\n  return f0 + (1.0 - f0) * m2 * m2 * m;\n}\n\n/* Exact unpolarised Fresnel. eta = n_incident / n_transmitted (the SAME\n   convention GLSL refract() takes — Lagarde's memo uses the reciprocal; mixing\n   the two silently inverts every interface). Returns EXACTLY 1.0 past the\n   critical angle, which is what makes TIR need no branch downstream. */\nfloat fresnelExact(float cosI, float eta){\n  float c  = clamp(abs(cosI), 0.0, 1.0);\n  float s2 = eta * eta * (1.0 - c * c);\n  if (s2 >= 1.0) return 1.0;\n  float ct = sqrt(1.0 - s2);\n  float rs = (eta * c - ct) / (eta * c + ct);\n  float rp = (c - eta * ct) / (c + eta * ct);\n  return 0.5 * (rs * rs + rp * rp);\n}\n\nvec3 lin2srgb(vec3 c){\n  c = clamp(c, 0.0, 1.0);\n  return mix(c * 12.92,\n             1.055 * pow(max(c, vec3(1e-5)), vec3(0.41666)) - 0.055,\n             step(vec3(0.0031308), c));\n}\n\nstruct Slab { float dz; float path; float ct; vec3 T; vec3 Nb; float hit; };\n\nSlab slabAt(vec3 N, vec3 V, float n){\n  Slab s;\n  s.T  = refract(-V, N, 1.0 / n);\n  /* Entering air->glass eta = 1/n < 1, so refract() can NEVER return the zero\n     vector here and -T.z is bounded below by cos(asin(1/n)) = sqrt(1 - 1/n^2)\n     = 0.7531 at n=1.52. This clamp guards a degenerate normal, NOT TIR.\n     Consequence: path length is bounded at 1.33x dz. It cannot blow up. */\n  s.ct = max(-s.T.z, sqrt(max(1.0 - 1.0/(n*n), 0.0)) * 0.9);\n\n  vec2 su = gl_FragCoord.xy / uRes;\n  vec2 bk = backRescue(su);\n  s.hit  = bk.y;\n  s.dz   = max(bk.x - (-vP.z), 0.0) * step(0.5, bk.y);\n  s.path = s.dz / s.ct;\n\n  vec4 raw = texture2D(uBack, su);\n  s.Nb   = vec3(raw.g, raw.b, 0.0);\n  s.Nb.z = -sqrt(max(1.0 - dot(s.Nb.xy, s.Nb.xy), 0.0));\n  return s;\n}\n\n/* KHR_materials_volume, normative: sigma_t = -log(c)/d, T(x) = exp(-sigma_t*x).\n   exp(log(c)/d * x) is the same thing with one negation saved.\n   uAtten MUST be < 1 or nothing happens: -log(1) = 0.\n   uAttenDist is in the SAME units as the geometry — design px here, so sane\n   values are 15..900, not the metre-scale numbers in the spec's example. */\nvec3 transmittance(float path){\n  if (uAttenDist <= 0.0) return vec3(1.0);\n  return exp(log(max(uAtten, vec3(1e-4))) / uAttenDist * path);\n}\n```\n\n### A.5 The silhouette, part 2 — and why it is not a bug\n\nAt the true outline the front and back surfaces meet, so `dz → 0` and the refraction genuinely goes to zero. **Do not floor it.** The three mechanisms that must be right there are:\n\n1. **Never invent thickness** — the hit flag (A.2) plus the rescue. This is the only actual bug.\n2. **Let path length carry it.** `path = dz / ct` and `ct` is bounded at `cos(asin(1/n))`, so `path ≤ 1.33·dz`. The rim thins smoothly rather than snapping.\n3. **The rim goes opaque instead.** At the silhouette the surface is edge-on, front Fresnel climbs toward 1, and the back interface is deep inside its TIR cone. Section D turns exactly that into mask opacity. **The refraction dying at the silhouette is the cue that the edge is a mirror, not a window** — it is the effect, not a hole to patch.\n\nThere is one thing you must not do: `dz = mix(uThickConstant, dz, hit)`. Falling back to a constant slab invents a full-depth lens on the one-pixel sliver where the mask says there is barely any glass, which is the same artefact as 0.2 with a nicer name.\n\n### A.6 Call-site changes in `renderBack` (`index.html:8748`)\n\n```js\nuni.uRes.value.set(w, h);\nuni.uTexel.value.set(1 / w, 1 / h);      // NEW\nvar keep = mesh.material;\nmesh.material = matBack;\nrenderer.setRenderTarget(backRT);\nrenderer.setClearColor(0x000000, 0);      // CHANGED from alpha 1\nrenderer.clear();\nrenderer.render(scene, camera);\nrenderer.setRenderTarget(null);\nmesh.material = keep;\nuni.uBack.value = backRT.texture;         // MOVED to after the render — never\n                                          // leave the RT bound as a sampler\n                                          // while it is the draw target\n```\n\n---\n\n## B. The corrected ENCODER, in full\n\n```glsl\nprecision highp float;\n/* [PRELUDE FROM A.4 GOES HERE] */\n\n/* three's FRAGMENT prefix declares ONLY viewMatrix, cameraPosition and\n   isOrthographic (WebGLProgram.js:767-769). projectionMatrix is prefixVERTEX\n   only (line 599). Referencing it in a fragment shader without this line is an\n   undeclared identifier and the program fails to link — and this codebase\n   swallows that, because the try/catch around the three import means the ring\n   just stays as the flat film with no error in the console. VERIFIED: removing\n   this one line makes glslangValidator emit\n       ERROR: 'projectionMatrix' : undeclared identifier\n   Declaring it is safe AND sufficient: WebGLRenderer.setProgram calls\n       p_uniforms.setValue(_gl, 'projectionMatrix', camera.projectionMatrix)\n   by NAME LOOKUP on the linked program, with no condition on which stage\n   declared it. */\nuniform mat4  projectionMatrix;\nuniform float uBend, uRange, uWarp, uSignX, uSignY, uOut;\n\nvoid main(){\n  vec3 N = normalize(vN);\n  vec3 I = normalize(vP);                    // camera -> surface\n  if (dot(N, I) > 0.0) N = -N;               // always face the camera\n  vec3  V = -I;\n  float n = max(uIor, 1.0001);\n\n  Slab  s  = slabAt(N, V, n);\n  float dz = s.dz;\n\n  /* REFINEMENT. dz from a single tap assumes the exit surface is a plane\n     parallel to the image plane. On the bevel it is turned hard away. Walk to\n     the first-guess exit point, re-sample the back buffer THERE, re-solve.\n     refract() returns a unit vector for unit inputs, so stepping dz/ct along T\n     lands exactly dz deeper in Z. Two iterations converge on this geometry.\n     GUARD cp.w > 0: a grazing ray can put the estimate behind the eye, where\n     the perspective divide flips the UV into a garbage quadrant. */\n  for (int i = 0; i < 2; i++){\n    vec3 Pe = vP + s.T * (dz / s.ct);\n    vec4 cp = projectionMatrix * vec4(Pe, 1.0);\n    if (cp.w > 0.0){\n      vec2 eu = cp.xy / cp.w * 0.5 + 0.5;\n      if (eu.x >= 0.0 && eu.x <= 1.0 && eu.y >= 0.0 && eu.y <= 1.0){\n        vec2 b2 = backAt(eu);\n        if (b2.y > 0.5) dz = max(b2.x - (-vP.z), 0.0);\n      }\n    }\n  }\n  float path = dz / s.ct;\n\n  /* ---- OUTPUT 1: THE TRANSMITTANCE MAP -------------------------------\n     What actually reaches the eye through the glass is\n         (1 - F_front) * exp(-sigma * path) * (1 - F_back)\n     Both interfaces, not just the medium. This is the ONLY place Beer-Lambert\n     can touch the DOM — see C.0.\n     Cleared to OPAQUE WHITE. Alpha 1 is not cosmetic: feComposite arithmetic\n     operates on PREMULTIPLIED values, so a transparent pixel would zero the\n     product and punch a hole in the backdrop.\n     sRGB-ENCODED, because the feComposite is marked linearRGB and the browser\n     applies the sRGB EOTF to that feImage on the way in. The round trip then\n     cancels exactly. Leave it linear AND ask for linearRGB and you linearise\n     twice and the absorption renders ~2.2x too dark. These two ship together\n     or neither ships. */\n  if (uOut > 0.5){\n    float f0 = pow((n - 1.0) / (n + 1.0), 2.0);\n    float Ff = F_Schlick(clamp(dot(N, V), 0.0, 1.0), f0);\n    float Fb = fresnelExact(abs(dot(s.T, s.Nb)), n) * step(0.5, s.hit);\n    vec3  tr = transmittance(path) * (1.0 - Ff) * (1.0 - Fb);\n    gl_FragColor = vec4(lin2srgb(tr), 1.0);\n    return;\n  }\n\n  /* ---- OUTPUT 0: THE DISPLACEMENT MAP --------------------------------\n     off = (T.xy / cos) * dz is the EXACT slab result. -T.z is the cosine\n     against the slab normal, so dividing by it IS the 1/cos path lengthening,\n     and multiplying by the axial depth gives the lateral drift before exit.\n     Constant thickness made every part of the ring drift the same amount. dz\n     makes the thin gear teeth barely bend and the fat outer wall bend hard,\n     which is the entire point.\n\n     NOTE THE CONVENTION. three's own `thickness` is ARC LENGTH along the\n     refracted ray (getVolumeTransmissionRay: normalize(refractionVector) *\n     thickness * modelScale). Ours is AXIAL DEPTH. They differ by 1/cos. Use dz\n     for the walk and `path` for Beer-Lambert. Copying three's formula while\n     keeping this convention under-refracts everywhere the bevel turns. */\n  vec2 off = (s.T.xy / s.ct) * dz * uBend;\n\n  /* WARP — the one deliberately unphysical term, and it stays. A real thick\n     slab displaces its flat face UNIFORMLY, which is invisible; all the\n     lensing comes from the bevel. Warp is what makes the mark read as a lens\n     with a body rather than a bevelled window. Cubic in radius so the middle\n     stays honest. It IS dispersed downstream with everything else, which is\n     correct — a real lens disperses. */\n  off += vRad * dot(vRad, vRad) * uWarp;\n\n  vec2 e = clamp(vec2(off.x * uSignX, off.y * uSignY) / max(uRange, 1e-3),\n                 -1.0, 1.0) * 0.5 + 0.5;\n\n  /* B was `1.0 - abs(N.z)`, a stand-in for optical thickness. It is the real\n     normalised path length now. Nothing in the SVG reads B; this is for\n     eyeballing a dumped map. */\n  gl_FragColor = vec4(e.x, e.y, clamp(path / max(uRange, 1e-3), 0.0, 1.0), 1.0);\n}\n```\n\n### B.1 Per-channel IOR dispersion — where it belongs, and why\n\nThe encoder has exactly two channels of offset, and all three `feDisplacementMap` primitives read the same R and G at different **scalar** scales. So per-channel IOR cannot be encoded into the map. The question is whether a global scale ratio is good enough.\n\nIt is, and by a wide margin. Lateral displacement through a slab is `dz·tan(θt)` with `θt = asin(sinθi/n)`. I evaluated the ratio `d(nR)/d(nG)` and `d(nB)/d(nG)` across the full incidence range:\n\n```\nangle   ratio R/G     ratio B/G\n   5    1.002590      0.994816\n  30    1.002895      0.994213\n  60    1.003824      0.992389\n  85    1.004527      0.991016\n  variation of the R ratio across 5..85 deg:  0.193 %\n  variation of the B ratio across 5..85 deg:  0.383 %\n```\n\n**The ratio is constant to 0.4% over the entire object.** The `dd/d = dn/(n(n−1))` cancellation is exact to first order and holds to fourth-decimal accuracy in practice. Keeping the split in the three SVG scales is correct, not a compromise.\n\nWhat is wrong today is that the split is symmetric. Derive it instead, in `applyEncoded` (`index.html:9048`):\n\n```js\n/* Abbe number pinned at V = 45 — a mid crown/flint, enough fire to be visible\n   without a second slider. Total red->blue fractional spread has a closed form:\n       D = 1.0237 / (V * n_d)\n   (1.0237 = the 612..465 nm span of 1/lambda^2 over the F..C span.)\n   At ior 1.52 that is 0.01497 — REAL GLASS DISPERSES BY ABOUT ONE PERCENT.\n   Anything visible on a screen is a 10-30x exaggeration, which is exactly what\n   `fire` is, and why it is stated as a gain rather than hidden in a constant.\n\n   The weights are the normalised per-channel deviations at the sRGB primary\n   dominant wavelengths 612/549/465 nm. BLUE MOVES 2.017x AS FAR FROM GREEN AS\n   RED DOES. The old symmetric (1-c, 1, 1+c) dragged green off-centre by half\n   the red-blue spread, which tints the whole refracted image instead of\n   fringing only its edges, and made the red and blue fringes equal width where\n   real glass has a wide blue fringe and a narrow red one. */\nvar V_ABBE = 45.0;\nvar D  = 1.0237 / (V_ABBE * Math.max(1.0001, P.ior));\nvar WR = -0.33144, WB = 0.66856;\nvar S  = 2 * RANGE * u;\nfeDisp[0].setAttribute('scale', (S * (1 + D * P.fire * WR)).toFixed(3));\nfeDisp[1].setAttribute('scale', (S).toFixed(3));\nfeDisp[2].setAttribute('scale', (S * (1 + D * P.fire * WB)).toFixed(3));\n```\n\nAt `ior 1.52, fire 9` this gives `0.9554 / 1.0 / 1.0900` — very close in magnitude to today's `chroma 0.055` (`0.945 / 1.0 / 1.055`) but asymmetric the right way round.\n\n### B.2 The measured `range` bound\n\n`encode()` at `index.html:8988` computes the ceiling from `P.thick * P.scale`, which no longer exists. Replace with the geometric Z extent:\n\n```js\nvar crit  = Math.asin(Math.min(0.999, 1 / Math.max(1.0001, P.ior)));\nvar zSpan = (P.depth + 2 * P.bevelT) * P.scale;      // the real solid depth\nvar bound = zSpan * Math.tan(crit) * Math.abs(P.bend) + Math.abs(P.warp) + 1;\n```\n\nAt the new defaults: `68.2 × 0.8736 × 1.0 + 42 + 1 = 102.6` design px on a 440 px mark. The two-draw measure-then-re-encode stays exactly as it is — it is the right design and it is what keeps the 8 bits spanning the range actually in use.\n\n---\n\n## C. The corrected LIT pass, in full\n\n### C.0 Read this before writing any of it\n\n**The lit canvas is `mix-blend-mode: screen`. Screen can only add. Absorption cannot darken the DOM from here.** The `absorb` term currently in `LIT_F` is only attenuating the glass's own reflections — which is correct and should stay — but the transmitted path needs the transmittance map from B and the `feComposite` in C.4. Those two are one feature; shipping the lit pass alone leaves the ring exactly as unmaterial as it is now.\n\n### C.1 The shader\n\n```glsl\nprecision highp float;\n/* [PRELUDE FROM A.4 GOES HERE] */\n\nuniform vec3  uLightA, uLightB, uSpecA, uSpecB, uEnvTop, uEnvBot;\nuniform float uRough, uGlow;\n\n/* Filament D_GGX (highp form). three gives the fragment stage highp on\n   desktop, so this is the one to use — the cross-product mediump variant only\n   exists to dodge the catastrophic cancellation in 1 - NoH*NoH as NoH -> 1. */\nfloat D_GGX(float NoH, float a){\n  float a2 = a*a; float f = (NoH*a2 - NoH)*NoH + 1.0;\n  return a2 / (PI * f * f);\n}\n/* Filament V_SmithGGXCorrelated, height-correlated, exact form.\n   V = G / (4 (n.v)(n.l)) — THE 4*NoV*NoL DENOMINATOR IS ALREADY IN HERE.\n   Dividing by it again after D*V*F is the classic double-divide bug: it blows\n   up at grazing angles and gives a white rim that looks like a Fresnel effect\n   and is not one. */\nfloat V_SmithGGX(float NoV, float NoL, float a){\n  float a2 = a*a;\n  float gv = NoL * sqrt(NoV*NoV*(1.0-a2) + a2);\n  float gl = NoV * sqrt(NoL*NoL*(1.0-a2) + a2);\n  return 0.5 / max(gv + gl, 1e-5);\n}\nvec3 ggx(vec3 N, vec3 V, vec3 L, float a, float f0, vec3 lc){\n  vec3  H   = normalize(V + L);\n  float NoV = abs(dot(N, V)) + 1e-5;\n  float NoL = clamp(dot(N, L), 0.0, 1.0);\n  float NoH = clamp(dot(N, H), 0.0, 1.0);\n  float LoH = clamp(dot(L, H), 0.0, 1.0);\n  /* Fresnel inside the lobe takes LoH, NOT NoV. Using NoV is subtle, common,\n     and makes the highlight the wrong brightness at oblique light angles. */\n  return vec3(D_GGX(NoH, a) * V_SmithGGX(NoV, NoL, a) * F_Schlick(LoH, f0)) * lc * NoL;\n}\n\n/* THE ENVIRONMENT. There isn't one — the page is #141414 — and that is exactly\n   the problem the old ad-hoc fresnel was papering over: a 4% reflection of\n   nothing is nothing. So synthesise the setup glass is actually PHOTOGRAPHED\n   in: dark-field. A large soft source above and behind, a dark ground, and the\n   subject's edges catch it. Two colours and a squared vertical ramp is the\n   whole model, and it is the difference between a Fresnel term that reads as\n   material and one that reads as a vignette. */\nvec3 envAt(vec3 R){\n  float t = clamp(R.y * 0.5 + 0.5, 0.0, 1.0);\n  return mix(uEnvBot, uEnvTop, t * t);\n}\n\nvoid main(){\n  vec3 N = normalize(vN);\n  vec3 V = normalize(-vP);\n  if (dot(N, V) < 0.0) N = -N;\n\n  float n   = max(uIor, 1.0001);\n  /* F0 = ((n-1)/(n+1))^2 — the ONLY correct source for a dielectric's\n     reflectance. 1.52 -> 0.0426. 1.45 -> 0.0337. The 1.185 that is in the file\n     today -> 0.0072, which is not a material, it is a strength knob. */\n  float f0  = pow((n - 1.0) / (n + 1.0), 2.0);\n  float pr  = clamp(uRough, 0.045, 1.0);     // fp32 floor (Frostbite's number);\n  float a   = pr * pr;                        // 0.089 is the fp16 floor, unneeded here\n  float NoV = clamp(dot(N, V), 0.0, 1.0);\n\n  Slab s  = slabAt(N, V, n);\n  vec3  tr = transmittance(s.path);\n\n  /* ---- FRONT INTERFACE. Schlick is correct here and exact Fresnel is not\n     worth it: eta = 1/n < 1, so there is no critical angle on a front face and\n     Schlick's worst error is ~3.6% at 85 degrees. */\n  float Ff  = F_Schlick(NoV, f0);\n  vec3  col = envAt(reflect(-V, N)) * Ff;\n\n  /* ---- BACK INTERFACE. Here eta = n > 1 and TIR is real: past\n     theta_c = asin(1/n) = 41.14 deg at n=1.52, reflectance is EXACTLY 1 and\n     the interface is a perfect mirror. That bright filament along the bevel IS\n     the look of thick glass — it is the feature, not the error path.\n\n     Schlick MUST NOT be used here. It reaches 1.0 only at 90 degrees, so\n     across the whole ~49-degree TIR cone it under-reflects and leaves a hard\n     visible seam exactly at theta_c. fresnelExact returns exactly 1.0 there,\n     so the transition is continuous and TIR needs no branch.\n\n     What the mirrored ray carries: the env, plus the key light's GGX lobe\n     evaluated against the BACK normal at f0 = 1 (past theta_c the interface IS\n     a mirror). On a black page the env alone contributes nothing, so without\n     the second term there is no filament. */\n  vec3  Vb  = -s.T;\n  vec3  Nbf = dot(s.Nb, Vb) < 0.0 ? -s.Nb : s.Nb;\n  float Fb  = fresnelExact(abs(dot(s.T, s.Nb)), n) * step(0.5, s.hit);\n  col += (envAt(reflect(s.T, s.Nb))\n        + ggx(Nbf, Vb, normalize(uLightA), a, 1.0, uSpecA))\n        * (Fb * (1.0 - Ff)) * tr;     // <- absorbed on the way back out\n\n  /* ---- DIRECT HITS. Two lights, because 95% of glass is lighting and one\n     soft highlight is the plastic look. Values are allowed well past 1.0. */\n  col += ggx(N, V, normalize(uLightA), a, f0, uSpecA);\n  col += ggx(N, V, normalize(uLightB), a, f0, uSpecB);\n\n  /* ---- OUTPUT TRANSFER. renderer.outputColorSpace is LinearSRGBColorSpace\n     (index.html:8482) and MUST STAY THAT WAY — the encoder writes numbers, not\n     colour, and an sRGB transfer on the way out would bend every one of them.\n     So the LIT pass applies its own OETF here. Without this line the canvas\n     writes linear values into a buffer CSS composites as sRGB and everything\n     lands 5-13x too dark: an intended 0.04 shows as 0.0031, an intended 0.10\n     as 0.0100. That is the entire reason `bright: 1.41` and specI/fresI/rimI\n     past 1.0 exist. F0 = 0.04 correctly encoded is 0.2209 — a visible sheen on\n     black, which is what glass looks like. */\n  gl_FragColor = vec4(lin2srgb(max(col * uGlow, 0.0)), 1.0);\n}\n```\n\n### C.2 Constants (not sliders)\n\n```js\nuEnvTop: new THREE.Vector3(0.100, 0.115, 0.145),   // the card above\nuEnvBot: new THREE.Vector3(0.012, 0.012, 0.016),   // near-black ground\nuSpecA:  new THREE.Vector3(1.00, 0.97, 0.92),      // warm key\nuSpecB:  new THREE.Vector3(0.26, 0.31, 0.42),      // cool kicker, pre-weighted 0.42\nuAtten:  uni.uTintB.value.clone(),                 // the Figma dark stop, LINEAR\n```\n\n`uAtten` note: `new THREE.Color(0x948880)` with colour management on gives **linear** (0.2918, 0.2423, 0.2140), luminance 0.2508, `−log = 1.383`. Do not \"fix\" that to the sRGB triplet — Beer-Lambert is a linear-light operation and three has already done the right conversion.\n\n`uLightB` is derived from `uLightA`, not exposed: `normalize(vec3(-LA.x, -LA.y * 0.35, 0.8))`.\n\n### C.3 The value distribution that reads as glass on a near-black page\n\nScreen-blended over `#141414`, targets measurable with the existing `tools/shot.js`:\n\n- Median inside the silhouette: **0.05 – 0.15**\n- Above 0.8: **under 3%** of mark pixels\n- Genuinely clipped (1.0): **a few dozen pixels**, all on the bevel\n- Flat-face floor: `F0` sRGB-encoded ≈ **0.22** times `uEnvTop`, i.e. ~0.025 — present, not zero\n- `uGlow` under ~0.03 of extra body lift; buy legibility from the outer bloom and the edge filament, never from raising the interior\n\nThe instinct on a black page is to lift the body because it looks empty. That is precisely what kills it. Glass on black is a low-key image with a handful of clipped hits.\n\n### C.4 The SVG change absorption requires\n\n`feDisplacementMap` only moves pixels; it cannot darken. Beer-Lambert must **multiply**, and a sibling div with `mix-blend-mode: multiply` cannot work — GLASS.md already records that a `backdrop-filter`'s output is not part of its group's blend backdrop. So it goes inside the same chain.\n\nInsert before `feMap` (`index.html:8432`):\n\n```js\nvar feAbs = el('feImage', { preserveAspectRatio: 'none', result: 'ABS',\n                            x: 0, y: 0, width: 1, height: 1 });\nfilter.insertBefore(feAbs, filter.firstChild);   // primitives run in document order\n```\n\nName the last blend (`index.html:8451`) and composite:\n\n```js\nfilter.appendChild(el('feBlend', { in: 'CRG', in2: 'CB', mode: 'screen',\n                                   result: 'DISP' }));\n/* result = k1*i1*i2 + k2*i1 + k3*i2 + k4.  k1=1, rest 0  ==  i1 * i2.\n   Operates on PREMULTIPLIED values, which is why the ABS map must be opaque —\n   and why the alpha arithmetic works out to 1*1*1 = 1, leaving the backdrop\n   opaque. Do NOT be tempted to store (1 - transmittance) and use k1=-1,k2=1:\n   the same k's apply to alpha, giving a1*(1-a2) = 0, which punches the glass\n   straight through the page. */\nfilter.appendChild(el('feComposite', {\n  in: 'DISP', in2: 'ABS',\n  operator: 'arithmetic', k1: '1', k2: '0', k3: '0', k4: '0',\n  /* linearRGB on THIS PRIMITIVE ONLY. The <filter> stays sRGB, which is right\n     for the displacement chain (those are encoded numbers). A Beer multiply is\n     linear-light. Measured: an sRGB-space multiply is too LIGHT, not too dark\n     — 0.25 x 0.25 gives 0.0625 where linear-correct is 0.0334, nearly 2x too\n     bright in the darks, i.e. it UNDER-absorbs. This is the opposite of the\n     usual folklore, which is about AVERAGING, not multiplying. Pairs with the\n     lin2srgb() in the encoder's uOut branch; ship both or neither. */\n  'color-interpolation-filters': 'linearRGB'\n}));\n```\n\nIn `applyEncoded` (`index.html:9024`), rewrite the ABS subregion on **every** regeneration, in CSS px:\n\n```js\nfeAbs.setAttribute('x', 0); feAbs.setAttribute('y', 0);\nfeAbs.setAttribute('width',  W.toFixed(2));\nfeAbs.setAttribute('height', H.toFixed(2));\nfeAbs.setAttribute('href', e.abs);\nfeAbs.setAttributeNS('http://www.w3.org/1999/xlink', 'xlink:href', e.abs);\n```\n\n`primitiveUnits` is `userSpaceOnUse` (the existing comment at `index.html:8425` says so), so `x=0 y=0 width=1 height=1` set once is a **one-pixel subregion**. `feMap` only works today because `applyMap` rewrites those four attributes every time; ABS must do the same.\n\nCost: one extra `toDataURL` per parameter change, and one extra raster in a chain Chrome re-evaluates every frame the ring moves. If it drags, `polish` to 1 first (drops the CSS blur), then `MAPRES`.\n\n---\n\n## D. The corrected MASK / edge shader, in full\n\n```glsl\nprecision highp float;\n/* [PRELUDE FROM A.4 GOES HERE] */\nuniform float uEdge;\n\nvoid main(){\n  vec3 N = normalize(vN);\n  vec3 V = normalize(-vP);\n  if (dot(N, V) < 0.0) N = -N;\n  float n  = max(uIor, 1.0001);\n  float f0 = pow((n - 1.0) / (n + 1.0), 2.0);\n\n  Slab  s   = slabAt(N, V, n);\n  float Ff  = F_Schlick(clamp(dot(N, V), 0.0, 1.0), f0);\n  float Fb  = fresnelExact(abs(dot(s.T, s.Nb)), n) * step(0.5, s.hit);\n  float trL = dot(transmittance(s.path), vec3(0.2126, 0.7152, 0.0722));\n\n  /* Exactly three's own rule — as transmittance falls, ALPHA RISES:\n         1.0 - (1.0 - transmittedLight.a) * transmittanceFactor\n     Thick glass goes opaque as well as dark. Three real mechanisms, none of\n     them an invented exponent:\n       Ff  — the front face turns to a mirror at grazing incidence\n       Fb  — the back face is inside its TIR cone and reflects EVERYTHING\n       trL — the medium has already absorbed most of what got in\n     On a ring seen face-on the absorption does the heavy lifting through the\n     wall; on the bevel the TIR term takes over. That is why the old\n     pow(1-NdotV, edgeW) needed an exponent well below the physical 5 to give\n     the edge any thickness at all — it was the only term present, and it was\n     standing in for the other two. */\n  float opaque = 1.0 - (1.0 - Ff) * (1.0 - Fb) * trL;\n\n  /* A = coverage (the refraction is cut to this).\n     LUMINANCE = edge opacity (the film is cut to this via mask-mode).\n     uEdge is the honest free parameter: on a page with no environment, how\n     much light the mirror actually has to mirror is a design decision. */\n  gl_FragColor = vec4(vec3(clamp(opaque * uEdge, 0.0, 1.0)), 1.0);\n}\n```\n\nKeep `side: THREE.DoubleSide` — a steep tilt must not open a hole in the silhouette. It is also what keeps `flipSided = false`, so nothing here inherits the BackSide winding inversion.\n\n### D.1 Two required companion changes\n\n**CSS (`index.html:1377`, `1384`).** The film's gradient is `rgba(...,.4) → rgba(...,.5)` under `opacity: filmO`, which caps the achievable edge opacity at ~0.25. The mask now carries the whole modulation, so the layer must be able to reach 1.0:\n\n```css\n.off__glass--film{\n  opacity: 1;                       /* was var(--gfilm, .2); filmO is deleted */\n  background: linear-gradient(162.2deg, #FAE5D8 0%, #948880 100%);\n}\n```\n\n**Mask luminance colour space.** Chrome computes CSS `mask-mode: luminance` from the stored sRGB values directly (SVG `<mask>` elements default to linearRGB; CSS masks do not). So the mask value is written **raw**, with no transfer function — do not add `lin2srgb` here. This one is worth probing rather than trusting: render a flat 0.5 mask, measure the resulting film opacity, and confirm it lands at 0.5 and not 0.21.\n\n---\n\n## E. The slider set\n\nTen, named the way a designer would.\n\n| key | label | min | max | step | default |\n|---|---|---|---|---|---|\n| `ior` | glass density | 1.30 | 1.90 | 0.005 | **1.520** |\n| `bend` | how much it bends the page | 0 | 2.5 | 0.01 | 1.00 |\n| `absorb` | how much it drinks light | 0 | 1 | 0.005 | 0.28 |\n| `fire` | rainbow in the edges | 0 | 40 | 0.5 | 9 |\n| `polish` | frosted ← → mirror | 0 | 1 | 0.01 | 0.86 |\n| `warp` | lens bulge (px) | −60 | 60 | 1 | 42 |\n| `glow` | how lit it is | 0 | 3 | 0.01 | 1.00 |\n| `edge` | how solid the rim goes | 0 | 1 | 0.01 | 0.70 |\n| `lightA` | light angle | 0 | 360 | 1 | 96 |\n| `lightE` | light height | −90 | 90 | 1 | 44 |\n\nDerived, never exposed:\n\n```js\nuni.uRough.value     = 0.045 + (1 - P.polish) * 0.45;\nuni.uAttenDist.value = 14 + 900 * Math.pow(1 - P.absorb, 2.5);   // design px\nuni.uBend.value      = P.bend;         // DIMENSIONLESS. Never multiply by P.scale.\ncssBlurPx            = (1 - P.polish) * 9;\n// dispersion scales: see B.1\n```\n\n`polish` drives the GGX lobe **and** the CSS frost blur from one number, which is physically correct — they are the same microfacet roughness. `absorb` at 0.28 gives `attenDist ≈ 410`, which over the 68.2-unit solid transmits ~0.855 luminance, i.e. a ~22% film across the flat face. That is within a couple of points of the hand-tuned `filmO 0.55 × gradient 0.45 ≈ 0.25` sitting in the file today — the physics lands where the eye already put it, which is the check that says the model is right.\n\n### Verdict on every existing slider\n\n**DELETE (15 of the 19 named):**\n\n| slider | why it goes |\n|---|---|\n| `thick` | Stopped being a length when the back-face pass landed. Its refraction role merges into `bend`; its absorption role moves to `absorb`. It was also being multiplied by `P.scale` (`index.html:8869`) against a measured length that already scales — a double-scale bug. |\n| `gain` | Becomes `bend`. `thick × gain` were two knobs on one number. |\n| `chroma` | Replaced by `fire`. Dispersion is now derived from `ior` with the correct 2.017:1 blue:red asymmetry instead of a symmetric hand-dialled split. |\n| `blur` | Folded into `polish`. Frost and specular roughness are the same physical quantity and must not disagree. |\n| `sat` | A global grade on the backdrop that fights the absorption. |\n| `bright` | At 1.41 it is *lifting* the copy under the glass, when real glass with 4% front reflection plus absorption must darken it. It exists only to compensate defect 0.1. Fix the colour space and it is not merely redundant, it is wrong-signed. |\n| `specI` | Folded into `glow`. With GGX and F0 from IOR the relative weight is physics; only the exposure is taste. |\n| `specP` | GGX from `polish`. Blinn-Phong exponents of 20–60 are the plastic look; 300–900 is glass, and neither is a number anyone should be dialling. |\n| `fresI` | Folded into `glow`. |\n| `fresP` | The Schlick exponent is 5. It is not a parameter. |\n| `rimI` | The rim is TIR plus absorption now, not a harder-powered Fresnel. |\n| `rimP` | Same. |\n| `tintA` | The tint is the Beer-Lambert attenuation colour, taken from the Figma gradient. |\n| `filmO` | The mask luminance carries it; film opacity pins to 1 (D.1). |\n| `edgeW` | Replaced by `edge`, which gains a *computed* opacity rather than inventing an exponent to stand in for two missing mechanisms. |\n\n**KEEP:** `warp` (the one deliberately fake control, and it earns its place), `lightA`, `lightE`.\n\n**KEEP, meaning widens:** `ior`. It now drives F0, the critical angle, the dispersion spread and the refraction from one number. **Its default must move 1.185 → 1.52.** At 1.185 F0 is 0.72% and the critical angle 57.6° — that value was chosen to tame a constant-thickness lens that pushed too hard, and with real thickness plus a separate `bend` it is no longer doing that job.\n\n**Also delete (on the panel, not in the brief):** `dome` — a second fake normal-bend, redundant with `warp` once thickness is real. **Also keep:** `absorb`, rescaled per the mapping above.\n\n**Delete from `MAPKEYS`** (`index.html:9233`): `thick, gain, chroma, blur, sat, bright, edgeW, filmO, dome`. **Add:** `bend, fire, polish, glow, edge`.\n\n**Bump the storage key to `cnvrt.offglass.v3`.** The loader lets a saved value beat a new default, so without the bump nobody who has ever opened the panel will see any of this — the file already records that exact failure with `spinZ`.\n\n---\n\n## F. Implementation order, by visual gain per hour\n\n1. **`lin2srgb()` on the lit pass output** (C.1, last line). One line. Everything is currently 5–13× too dark and compensated by `bright: 1.41` and three intensity sliders past 1.0. Nothing else can be judged until this is right.\n2. **Back-RT clear alpha 1 → 0, plus the hit flag and rescue** (A.2, A.4). Kills the one-pixel ring of near-maximum displacement running around the entire silhouette. Unblocks 3, 5, 6.\n3. **Drop `P.scale` from `uThick`** (`index.html:8869`), collapse `thick`+`gain` into `bend`. One line and one rename; removes a size-dependent bug that reads as \"the glass looks thinner at larger sizes\".\n4. **Schlick-from-IOR plus the dark-field `envAt()`**, and raise `ior` 1.185 → 1.52 (C.1). This is where the body stops being a hole. Low reflectance reads as no glass, and a 0.72% reflection of a black page is nothing.\n5. **The transmittance map and the `feComposite`** (B `uOut` branch + C.4). The single biggest \"it is made of something\" change — the first point at which the ring genuinely darkens and tints what is behind it, and the copy stops being legible through the thick parts. Bigger than 6 and 7, and correspondingly the most work.\n6. **Back-face normals in `.gb`, and TIR** (A.3 + C.1 back-interface block). Free storage, and it produces the bright filament along the bevel that is the actual signature of thick glass.\n7. **The new mask** (D) plus the film CSS. Turns the rim opaque for three real reasons instead of one invented exponent.\n8. **GGX + Smith** replacing Blinn-Phong (C.1). Two hard lights, values past 1.0.\n9. **Dispersion derived from IOR** with the asymmetric weights (B.1). Subtle but it is what stops the fringes reading as a codec artefact.\n10. **The two refinement iterations** in the encoder (B). Only visible where the bevel turns hard away; do it last and confirm it earns its cost.\n11. **Cull the panel to the ten** (E), bump the storage key.\n\nOptional, only if a grazing look through both walls of the ring proves to matter: a dual-depth **accumulation** pass (`DoubleSide`, `depthTest:false`, additive `HalfFloatType`, front faces subtract z and back faces add) gives the total interior path for absorption where the nearest-back-face pass under-counts. Keep the nearest-back-face pass for the displacement walk regardless — the first interval is where the ray actually leaves. Note that `EXT_float_blend` is required for 32-bit float blending, so an accumulation target must be `HalfFloatType`.\n\n---\n\n## Verification\n\n- **Compile.** All three fragment shaders above were validated with `glslangValidator 11.7.0` as `#version 100` behind three r185's real `prefixFragment` (`precision highp float/int`, `viewMatrix`, `cameraPosition`, `isOrthographic`). Negative control: deleting the single line `uniform mat4 projectionMatrix;` from the encoder produces `ERROR: 'projectionMatrix' : undeclared identifier` and no code. That failure is silent in this codebase — the `try/catch` around the three import leaves the ring as the flat masked film with nothing in the console.\n- **Grey probe** (existing). The map's corner pixel must still read exactly `[128,128,128,255]`. `GREY` at `index.html:8921` is already built in linear space; do not touch it.\n- **Hit-flag probe** (new). Dump the back RT and confirm alpha is 0 outside the silhouette. If it is 1 everywhere, step 2 did not land.\n- **Absorption round-trip probe** (new). Bake a transmittance map at `uAtten = vec3(0.5)`, `uAttenDist` such that `path/d = 1`, and read back the PNG. It must be **188**, not 128 — 128 means the `lin2srgb` was omitted and the `linearRGB` composite will double-linearise it ~2.2× too dark.\n- **Histogram** (new). `node tools/shot.js` with an eval that measures the lit canvas inside the silhouette against the C.3 targets.\n- `python tools/verify-glass.py` still has to pass: every parameter one slider, every slider one parameter, no default outside its own range, every uniform the GLSL reads declared.",
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Research"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Verify"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Spec"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "res:thickness",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "a678927653a2b7d9b",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1786131750254,
      "queuedAt": 1786131745606,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Real (measured) thickness for a DOM-refracting glass object…",
      "promptPreview": "I refract LIVE DOM through a 3D glass object by rendering a screen-space displacement map in three.js (r185) and feeding it to an SVG feDisplacementMap via CSS backdrop-filter. Right now thickness is a CONSTANT slab. I need REAL thickness.\n\nResearch and give me exact, working technique for:\n1. Rendering back-face view-space depth into a WebGLRenderTarget (FloatType vs half-float vs packed RGBA dep…",
      "lastProgressAt": 1786132393137,
      "tokens": 83233,
      "toolCalls": 36,
      "durationMs": 642883,
      "resultPreview": "{\"topic\":\"Real (measured) thickness for a DOM-refracting glass object: back-face depth pass in three.js r185 → true path length → Beer-Lambert absorption → thickness-driven screen-space refraction walk, wired into an feDisplacementMap/backdrop-filter pipeline\",\"glsl\":\"/* =============================================================================\\n   REAL THICKNESS FOR THE OFFERINGS GLASS RING\\n …"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "res:fresnel-tir",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "a305deae92937651d",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1786131748748,
      "queuedAt": 1786131745606,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Physically-based real-time glass: IOR→F0 Schlick, exact unp…",
      "promptPreview": "For a physically-based real-time glass shader, give me the exact maths and GLSL for:\n1. Schlick's approximation with F0 derived from IOR: F0 = ((n1-n2)/(n1+n2))^2. For glass n=1.5 that is 0.04. Give the full GLSL including the ior->F0 conversion, and explain what F0 is for ior 1.185 vs 1.5 vs 2.4.\n2. The FULL Fresnel equations (not Schlick) for unpolarised light — Rs and Rp — and whether the extra…",
      "lastProgressAt": 1786132329513,
      "tokens": 71280,
      "toolCalls": 29,
      "durationMs": 580765,
      "resultPreview": "{\"topic\":\"Physically-based real-time glass: IOR→F0 Schlick, exact unpolarised Fresnel, total internal reflection handling, Abbe/Cauchy dispersion, and GGX+Smith specular — GLSL ES 1.00 for three.js ShaderMaterial\",\"glsl\":\"// =============================================================================\\n//  PHYSICALLY-BASED GLASS  —  GLSL ES 1.00  (three.js ShaderMaterial)\\n//  No #version, no in/o…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "res:look",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "a71860f9f28ea5c49",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1786131749782,
      "queuedAt": 1786131745607,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Making CG glass read as real thick bevelled glass rather th…",
      "promptPreview": "Research what makes CG glass look REAL rather than plasticky, specifically for a thick, chunky, bevelled logo mark on a near-black page. Not physics derivations — the practical craft.\n\nSources: search for Blender/Octane/Redshift glass shader breakdowns, \"why does my glass look like plastic\", Filament and Substance material guides, Codrops and Tympanus WebGL glass demos, Vercel/Aave/Apple liquid gl…",
      "lastProgressAt": 1786132221876,
      "tokens": 63633,
      "toolCalls": 30,
      "durationMs": 472094,
      "resultPreview": "{\"topic\":\"Making CG glass read as real thick bevelled glass rather than plastic — practical craft for a chunky logo mark on a near-black page, in a single-pass screen-space WebGL shader\",\"glsl\":\"/* =============================================================================\\n   THICK BEVELLED GLASS MARK — single-pass screen-space\\n   GLSL ES 1.00 / three.js ShaderMaterial.  No #version, no textur…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "ver:look",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "afe3e3f17d098a4ed",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1786132226554,
      "queuedAt": 1786132223654,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Making CG glass read as real thick bevelled glass rather th…",
      "promptPreview": "Adversarially verify this before it goes into production GLSL. Re-check the three highest-risk claims against primary sources. Fix any GLSL that would not compile as GLSL ES 1.00 under three.js ShaderMaterial, any wrong formula, any invented API. Return the same shape, corrected.\n\n{\"topic\":\"Making CG glass read as real thick bevelled glass rather than plastic — practical craft for a chunky logo ma…",
      "lastProgressAt": 1786133137155,
      "tokens": 110151,
      "toolCalls": 28,
      "durationMs": 910600,
      "resultPreview": "{\"topic\":\"Making CG glass read as real thick bevelled glass rather than plastic — practical craft for a chunky logo mark on a near-black page, in a single-pass screen-space WebGL shader (adversarially verified; GLSL ES 1.00 validated with glslangValidator 11.7.0 in highp, mediump and OUTPUT_SRGB configurations)\",\"glsl\":\"/* ===========================================================================…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "ver:fresnel-tir",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "add960ba6ec6ccd0a",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1786132333586,
      "queuedAt": 1786132331381,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Physically-based real-time glass: IOR→F0 Schlick, exact unp…",
      "promptPreview": "Adversarially verify this before it goes into production GLSL. Re-check the three highest-risk claims against primary sources. Fix any GLSL that would not compile as GLSL ES 1.00 under three.js ShaderMaterial, any wrong formula, any invented API. Return the same shape, corrected.\n\n{\"topic\":\"Physically-based real-time glass: IOR→F0 Schlick, exact unpolarised Fresnel, total internal reflection handl…",
      "lastProgressAt": 1786133198445,
      "tokens": 108061,
      "toolCalls": 16,
      "durationMs": 864858,
      "resultPreview": "{\"topic\":\"Physically-based real-time glass: IOR→F0 Schlick, exact unpolarised Fresnel, total internal reflection handling, Abbe/Cauchy dispersion, and GGX+Smith specular — GLSL ES 1.00 for three.js ShaderMaterial (adversarially verified; three.js prefix, D_GGX attribution, screen-space refraction and diamond's Abbe number corrected)\",\"glsl\":\"// =====================================================…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "ver:thickness",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a2e14d70610acf764",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1786132399621,
      "queuedAt": 1786132394869,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Real (measured) thickness for a DOM-refracting glass object…",
      "promptPreview": "Adversarially verify this before it goes into production GLSL. Re-check the three highest-risk claims against primary sources. Fix any GLSL that would not compile as GLSL ES 1.00 under three.js ShaderMaterial, any wrong formula, any invented API. Return the same shape, corrected.\n\n{\"topic\":\"Real (measured) thickness for a DOM-refracting glass object: back-face depth pass in three.js r185 → true pa…",
      "lastProgressAt": 1786132972260,
      "tokens": 88466,
      "toolCalls": 19,
      "durationMs": 572637,
      "resultPreview": "{\"topic\":\"Real (measured) thickness for a DOM-refracting glass object: back-face depth pass in three.js r185 → true path length → Beer-Lambert absorption → thickness-driven screen-space refraction walk, wired into an feDisplacementMap/backdrop-filter pipeline — CORRECTED after primary-source verification (WebGLProgram.js prefixFragment, WebGLState.js setMaterial, KHR_materials_volume, and the live…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "spec",
      "phaseIndex": 3,
      "phaseTitle": "Spec",
      "agentId": "af60da6d69c967ae1",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1786133203022,
      "queuedAt": 1786133200541,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd \"D:/ultron jits/cnvrt website run 3\" && grep -n \"setClea…",
      "promptPreview": "Write ONE implementation spec for making an existing screen-space glass shader physically real.\n\nTHE EXISTING PIPELINE (do not redesign it, improve the shading inside it):\n- three.js r185 renders an extruded, bevelled 3D mark offscreen.\n- Pass A \"encoder\": fragment colour = screen-space refraction offset, R/G around 0.5, becomes a PNG -> SVG feImage -> 3x feDisplacementMap -> CSS backdrop-filter. …",
      "lastProgressAt": 1786134116678,
      "tokens": 171622,
      "toolCalls": 26,
      "durationMs": 913655,
      "resultPreview": "All three shaders compile clean as ESSL 1.00 behind the real r185 fragment prefix, and the negative control fails exactly where predicted (`ERROR: 'projectionMatrix' : undeclared identifier`). Here is the spec.\n\n---\n\n# Physically real glass in the existing 3-pass pipeline\n### Implementation spec — `index.html`, offerings glass ring\n\nEverything below is a delta against the code that is in the file …"
    }
  ],
  "totalTokens": 696446,
  "totalToolCalls": 184
}